{
  "packages": [
    "nodejs@20."
  ],
  "shell": {
    "init_hook": [
      "export PATH=$(pwd)/.pulumi/bin:$PATH",
      "make",
      "npm install"
    ],
    "scripts": {
      "test": [
        "echo \"Error: no test specified\" && exit 1"
      ]
    }
  }
}
